<%= form_with(model: orders_credit) do |form| %>
  <% if orders_credit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(orders_credit.errors.count, "error") %> prohibited this orders_credit from being saved:</h2>

      <ul>
        <% orders_credit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :credit_id, style: "display: block" %>
    <%= form.text_field :credit_id %>
  </div>

  <div>
    <%= form.label :order_status_id, style: "display: block" %>
    <%= form.text_field :order_status_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
