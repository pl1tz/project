<%= form_with(model: extra_name) do |form| %>
  <% if extra_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(extra_name.errors.count, "error") %> prohibited this extra_name from being saved:</h2>

      <ul>
        <% extra_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
