<%= form_with(model: drive_type) do |form| %>
  <% if drive_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(drive_type.errors.count, "error") %> prohibited this drive_type from being saved:</h2>

      <ul>
        <% drive_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
