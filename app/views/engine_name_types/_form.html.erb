<%= form_with(model: engine_name_type) do |form| %>
  <% if engine_name_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(engine_name_type.errors.count, "error") %> prohibited this engine_name_type from being saved:</h2>

      <ul>
        <% engine_name_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
